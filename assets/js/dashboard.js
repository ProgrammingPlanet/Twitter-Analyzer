

/*var dash = new Vue({

	el: '#dash',

	data: {
		peoples: {
			totaltweets: [],
			newFs: [],
			newUnFs: [],
			frs: [],
			fngs: [],
			arenotfb: [], //dash.fngs.filter(x=>!this.frs.includes(x)),
			inotfb: [],// this.frs.filter(x=>!this.fngs.includes(x)),
			bothside: []// this.fngs.filter(x=>this.frs.includes(x))
		}
	},

	methods: {
		
	}

})*/